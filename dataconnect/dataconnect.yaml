specVersion: "v1"
serviceId: "paymangementsystem"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "paymangementsystem-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
